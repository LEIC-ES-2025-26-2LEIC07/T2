
# TOO EXPENSIVE TO RUN ON EVERY COMMIT :(
# name: Deploy

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: read

# concurrency:
#   group: deploy-production
#   cancel-in-progress: true

# jobs:
#   android-artifact:
#     name: Android Release Artifact
#     runs-on: ubuntu-latest
#     timeout-minutes: 30

#     steps:
#       - name: Check out repository
#         uses: actions/checkout@v4

#       - name: Set up Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: "17"

#       - name: Set up Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           channel: stable
#           cache: true

#       - name: Install dependencies
#         run: flutter pub get

#       - name: Build release APK
#         env:
#           SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
#           SUPABASE_KEY: ${{ secrets.SB_PV_KEY }}
#         run: |
#           URL=$(echo "$SUPABASE_URL" | tr -d '\n\r')
#           KEY=$(echo "$SUPABASE_KEY" | tr -d '\n\r')
#           flutter build apk --release --dart-define="NEXT_PUBLIC_SUPABASE_URL=$URL" --dart-define="SB_PV_KEY=$KEY"

#       - name: Upload APK artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: android-release-apk
#           path: build/app/outputs/flutter-apk/app-release.apk
#           if-no-files-found: error
